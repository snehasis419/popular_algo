
#include<conio.h>
#include<iostream.h>
#include<string.h>
int main()
{
    int i,t,j;
    char a[10][20];
    printf("enter the names");
    for(i=0;i<20;i++)
    {
        scanf("%s",&a);
    }
    for(j=0;j<0;j++)
    {
        if (strcmp(a[j],a[j+i]);
        {
            strcpy(t,a[j]);
            strcpy(a[j],a[j+1]);
            strcpy(a[j]+1,t);
        }
    }
}
for(i=0;i<20;i++)
{
    scanf("%s",&a);
}
getch();
}
